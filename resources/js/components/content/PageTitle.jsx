import React from 'react'

const PageTitle = (props) => {
    return (
        <>
            <div className="">
                <h4 className="">{props.title}</h4>
            </div>
        </>
    )
}

export default PageTitle